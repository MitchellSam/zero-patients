{
  "name": "zero-patients",
  "private": true,
  "version": "0.1.0",
  "description": "Jackbox-style Pandemic clone — shared-screen board, phone controllers",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "run": "npm run dev -w @zero-patients/server & npm run dev -w @zero-patients/web"
  },
  "engines": {
    "node": ">=20"
  }
}
